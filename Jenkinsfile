pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(url: 'https://github.com/SuperYangAI/rocketmq-dashboard.git', branch: 'master', changelog: true, poll: true)
      }
    }

  }
}